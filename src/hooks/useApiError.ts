/**
 * useApiError —— 页面统一错误提示 hook（L1-1 · Bottom-Up）.
 *
 * 实现已下沉到 @lieshoucloud/ui，本文件仅做 re-export，保证历史调用点
 * （`import { useApiError } from '../hooks/useApiError'`）零改动。
 */
export { useApiError } from '@lieshoucloud/ui';

/**
 * Theme store —— 由 lieshou-core-web 提供（业务逻辑唯一源，2026-10 纯函数族下沉）.
 * 本地实现已上收 core-web；本文件保留导出路径兼容既有页面/测试。
 */
export { useThemeStore } from '@lieshoucloud/core-web';
export type { ResolvedTheme, ThemeMode } from '@lieshoucloud/core-web';
